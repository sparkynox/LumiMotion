package com.sparkynox.lumimotion.screen

/**
 * Single source of truth for button sizing/spacing so every LumiMotion menu
 * (title, settings, credits) lines up identically instead of each screen
 * picking its own numbers.
 */
object MenuLayout {
    const val BUTTON_WIDTH = 160
    const val BUTTON_HEIGHT = 20
    const val BUTTON_SPACING = 26
}
